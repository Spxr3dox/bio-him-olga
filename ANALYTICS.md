# Повна аналітика сайту — Ольга Журавель (репетитор з хімії та біології)

Дата аналізу: 3 вересня 2026 · Версія: реліз-кандидат
Метод: рендер у Chrome headless (десктоп 1440px + справжній мобільний viewport 390px через iframe),
вимірювання DOM/розмірів/тап-зон скриптом, ручний розрахунок контрастності за формулою WCAG 2.1.

---

## 1. Загальний висновок

Сайт **повністю відповідає технічному завданню** і готовий до публікації після
підстановки реального домену та Telegram-юзернейму.

Візуально сайт побудований як **єдина світла тема**: повітряний м'ятно-бірюзовий фон,
тональні переходи між секціями (білий → світло-м'ятний) без різких чорно-білих смуг.
Темний колір ТЗ `#0F172A` працює як колір тексту та **один акцентний блок** —
фінальна картка із закликом до дії.

| Напрям | Оцінка | Коментар |
|---|---|---|
| Відповідність ТЗ | 10/10 | усі 5 блоків, тексти дослівно, принцип «без форм» дотримано |
| Швидкість | 9/10 | 121 КБ разом із фото; головне вузьке місце — зовнішні шрифти Google |
| Мобільна версія | 10/10 | горизонтального скролу немає, усі тап-зони ≥ 40px, закріплена панель дзвінка |
| Доступність (WCAG AA) | 9/10 | усі перевірені пари кольорів проходять AA після виправлень |
| SEO (технічне) | 9/10 | мета, Open Graph, JSON-LD, sitemap, robots; чекає реальний домен |
| SEO (контентне) | 6/10 | мало тексту для органіки: немає міста, цін, відгуків, FAQ |
| Конверсія | 8/10 | 14 точок контакту; бракує соціальних доказів (відгуки, результати) |

---

## 2. Відповідність ТЗ — по пунктах

### 2.1 Дизайн і стилістика

| Вимога ТЗ | Статус | Як реалізовано |
|---|---|---|
| Смарагдовий / м'ятно-зелений `#2EC4B6`, `#10B981` | ✅ | змінні `--bio`, `--bio-2`; біологічний блок, картки, ДНК |
| Глибокий синій / бірюзовий `#00B4D8`, `#0F172A` | ✅ | `--chem` — хімічні блоки та графіка; `--ink` — заголовки, текст і фінальна темна картка |
| Теплий бурштиновий для CTA | ✅ | `#FFB703 → #F59E0B` — усі кнопки дзвінка |
| Моделі молекул, бензольні кільця | ✅ | бензольне кільце з атомами (обертається), логотип-молекула |
| Схеми ДНК | ✅ | подвійна спіраль із 20 «сходинками» у hero + іконка в блоці «Біологія» |
| Стилізовані клітини | ✅ | 2 клітини з ядром у hero, іконка-бейдж біля фото |
| Хімічний посуд (колби) | ✅ | колба з рідиною та бульбашками, що піднімаються |
| Тонкі анімовані фонові лінії («зв'язки») | ✅ | `<canvas>`: 26–78 вузлів, лінії між близькими — імітація хімічних зв'язків |
| Естетично, академічно, дружньо до абітурієнтів | ✅ | темний «науковий» hero + світлі повітряні секції |

### 2.2 Головний принцип CTA

| Вимога | Статус | Перевірка |
|---|---|---|
| **ПОВНА ВІДСУТНІСТЬ форм та полів запису** | ✅ | виміряно в DOM: `forms=0`, `inputs=0` (`<input>`, `<textarea>`, `<select>`) |
| Усі кнопки — на дзвінок або месенджери | ✅ | 8 × `tel:`, 3 × Telegram, 3 × Viber; інших цільових дій немає |
| Поведінка на комп'ютері | ✅ | розмітка `tel:` збережена на всіх номерах; на пристроях із мишею клік копіює номер (Windows більше не питає, чим відкрити посилання) |

### 2.3 Структура екранів

| Блок ТЗ | Статус | Примітка |
|---|---|---|
| Блок 1. Hero: заголовок, підзаголовок, CTA-кнопка з іконкою телефону | ✅ | тексти дослівно з ТЗ; кнопка відкриває набір номера |
| Блок 2. Переваги: 4 картки з таблиці | ✅ | збережено пару «Елемент/Інструмент» + «Заголовок картки» + опис |
| Блок 3. Про викладача + фото + CTA | ✅ | тексти дослівно; фото з папки вбудовано |
| Блок 4. Напрямки підготовки (3 позиції) | ✅ | описи з ТЗ + розкладка на підпункти для читабельності |
| Блок 5. Фінальний CTA + Telegram/Viber | ✅ | тексти дослівно, швидкі посилання додано |

### 2.4 Технічні рекомендації для розробника

| Вимога | Статус | Перевірка |
|---|---|---|
| `<a href="tel:+380680772607">` на всіх номерах | ✅ | 8 посилань, усі з правильним форматом; інших варіантів немає |
| Sticky header із номером і кнопкою | ✅ | `position: sticky`, номер видно на десктопі, кнопка — завжди |
| Велика зручна кнопка для великого пальця (80% мобільних) | ✅ | закріплена нижня панель 56px + hero-кнопка 70px на весь екран |

---

## 3. Технічні метрики (виміряно)

### 3.1 Вага сторінки

| Файл | Розмір | Після gzip |
|---|---|---|
| `index.html` | 31,8 КБ | **7,4 КБ** |
| `assets/css/style.css` | 25,2 КБ | **7,1 КБ** |
| `assets/js/main.js` | 9,2 КБ | **3,2 КБ** |
| `assets/img/olga-zhuravel.jpg` | 59,0 КБ | 56,6 КБ (JPEG вже стиснений) |
| `favicon.svg` | 0,4 КБ | 0,3 КБ |
| **Разом** | **≈ 126 КБ** | **≈ 75 КБ** |

Для порівняння: середня сторінка в українському сегменті — 1,8–2,5 МБ.
Сайт легший приблизно у 20 разів.

### 3.2 Запити та DOM

- **Власних запитів: 5** (HTML, CSS, JS, фото, favicon) — жодних фреймворків, jQuery, бібліотек.
- **Зовнішніх: 2 домени** — `fonts.googleapis.com` + `fonts.gstatic.com` (шрифти Manrope + Inter).
- **Вузлів DOM: 363** (норма для лендінга — 800–1500; поріг Lighthouse — 1400).
- Зображень на сторінці: 1. Вся інша графіка — інлайн-SVG (0 запитів).
- Висота сторінки на мобільному: 6785px (≈ 8 екранів) — оптимально для лендінга.

### 3.3 Очікувані Core Web Vitals

> Оцінка на основі виміряної ваги й структури; повний Lighthouse-аудит не запускався
> (потребує вихід у мережу з реального домену).

| Метрика | Прогноз | Обґрунтування |
|---|---|---|
| **LCP** | 1,0–1,6 с (4G) | LCP-елемент — заголовок H1; блокує лише CSS 6,4 КБ; фото — `lazy` і не в LCP |
| **CLS** | < 0,05 | у `<img>` вказані `width`/`height`; анімації тільки `transform`/`opacity` |
| **INP** | < 100 мс | немає важких обробників; canvas працює у `requestAnimationFrame` |
| **TBF/TTI** | миттєво | 2,3 КБ JS, `defer`, без блокуючих скриптів |

**Ризик для CLS:** шрифти Google із `display=swap` — при підміні fallback → Manrope
можливий мікрозсув заголовків. Усувається самохостингом шрифтів (див. §8).

---

## 4. Доступність (WCAG 2.1 AA)

### 4.1 Контрастність — розраховано вручну

| Елемент | Кольори | Коефіцієнт | Норма AA | Статус |
|---|---|---|---|---|
| Основний текст описів | `#546681` на білому | **5,8:1** | 4,5:1 | ✅ |
| Підзаголовок hero | `#B9CDE0` на `#0F172A` | **10,8:1** | 4,5:1 | ✅ |
| Кнопка дзвінка | `#20160A` на `#FFB703` | **10,2:1** | 4,5:1 | ✅ |
| Надпис «ПРО ВИКЛАДАЧА» | `#0B7D6D` на білому | **5,0:1** | 4,5:1 | ✅ *(було 3,5:1 — виправлено)* |
| Підписи карток | `#0369A1` / `#047857` / `#0F766E` / `#B45309` | **5,0–5,9:1** | 4,5:1 | ✅ *(було 2,5:1 — виправлено)* |
| Ім'я градієнтом (H2) | `#0B7D6D → #0891B2` на білому | **3,7:1** | 3:1 (великий текст) | ✅ |
| Дрібний текст футера | `#7F98B2` на темному | **5,9:1** | 4,5:1 | ✅ |

**Знайдено і виправлено 2 реальні порушення AA:** дрібні підписи-«ейбрау» та підписи
інструментів у картках мали контраст 2,5–3,5:1 (яскраві бірюзовий/зелений на білому).
Замінено на темні відповідники, яскраві кольори лишились для іконок і ліній (декор — виняток за WCAG).

### 4.2 Інші перевірки

| Критерій | Статус |
|---|---|
| `lang="uk"` | ✅ |
| `alt` у фото | ✅ описовий |
| Ієрархія заголовків | ✅ 1×H1 → 4×H2 → 7×H3, без пропусків рівнів |
| Клавіатурна навігація | ✅ усі елементи — нативні `<a>`/`<button>`, видимий фокус `outline` 3px |
| Посилання «Перейти до вмісту» | ✅ зʼявляється при Tab |
| Декоративна графіка | ✅ `aria-hidden="true"` на SVG-декорі та canvas |
| Стан меню для скрінрідерів | ✅ `aria-expanded`, `aria-controls`, зміна `aria-label` |
| `prefers-reduced-motion` | ✅ усі анімації та canvas вимикаються |
| Розмір тап-зон (390px) | ✅ виміряно: елементів менших за 40px — **0** |
| Робота без JS | ✅ контент видимий (анімацію появи вмикає сам JS) |

**Незначне зауваження:** мобільне меню не «замикає» фокус усередині (focus trap).
Для 4 пунктів меню це не критично, Escape і клік поза меню закривають його.

---

## 5. Мобільна версія (виміряно на 390px)

| Параметр | Результат |
|---|---|
| `scrollWidth` vs `clientWidth` | 390 = 390 → **горизонтального скролу немає** |
| Елементи, що виходять за екран | 4, усі — декоративні розмиті плями під `overflow:hidden` (не впливають) |
| Закріплена панель дзвінка | висота 56px + безпечна зона iPhone (`env(safe-area-inset-bottom)`) |
| Перекриття контенту панеллю | немає (`padding-bottom: 88px` у `body`) |
| Головна кнопка hero | на всю ширину екрана, висота 70px |
| Шапка | компактна: логотип + кругла кнопка дзвінка + бургер |

**Виправлено під час перевірки:** посилання «Зателефонуйте — підберемо разом» мало
`white-space: nowrap` і виходило за екран на 69px (текст обрізався). Тепер переноситься,
номер лишається нерозривним.

---

## 6. SEO

### 6.1 Що вже є

- `<title>` 68 символів, `description` 152 символи — обидва в межах видачі Google.
- Open Graph + Twitter Card (превʼю в Telegram, Viber, Facebook).
- **JSON-LD `Person`**: імʼя, посада, телефон, 10 тем (`knowsAbout`), 3 послуги (`makesOffer`).
- `robots.txt`, `sitemap.xml`, `canonical`, `theme-color`.
- Семантична розмітка: `header`/`main`/`section`/`article`/`footer`.
- Швидкість і мобільність — прямі фактори ранжування.

### 6.2 Що обмежує органіку

1. **Немає геоприв'язки.** Запити «репетитор з хімії Київ», «репетитор з біології онлайн»
   — у текстах немає ані міста, ані слова «онлайн». Це найдешевший приріст трафіку.
2. **Мало тексту** (~350 слів). Для конкуренції за НМТ-запити потрібно 800–1500 слів:
   блок FAQ, опис програми по темах, «як проходить заняття».
3. **Плейсхолдер-домен** `zhuravel-nmt.com.ua` у `canonical`, `og:url`, `sitemap.xml`.
   **Обовʼязково замінити перед публікацією** — інакше Google проіндексує чужу адресу.
4. **`og:image` відносний і квадратний.** Для превʼю потрібен абсолютний URL і формат 1200×630.
5. Немає розмітки `FAQPage` і `AggregateRating` — розширені сніпети недоступні.

---

## 7. Конверсія

### 7.1 Точки контакту: 14

| Місце | Тип | Помітність |
|---|---|---|
| Шапка (завжди на екрані) | номер + кнопка | висока |
| Hero | велика кнопка + Telegram + Viber | максимальна |
| Блок «Про викладача» | кнопка «Обговорити графік» | середня |
| Блок «Напрямки» | текстове посилання | середня |
| Фінальний блок | велика кнопка + 2 месенджери | максимальна |
| Футер | номер-кнопка | середня |
| Мобільна панель | кнопка + 2 іконки, завжди на екрані | максимальна |

Відстань між CTA — не більше одного екрана прокрутки на будь-якому пристрої.

### 7.2 Сильні сторони

- Одна цільова дія (дзвінок) — немає розпорошення уваги.
- Відсутність форм прибирає бар'єр «залишити дані» і питання згоди на обробку даних.
- Конкретна обіцянка результату («180+», «вступ на бюджет») у першому екрані.
- Дефіцит («кількість місць обмежена») у фінальному блоці.

### 7.3 Чого бракує для зростання конверсії

| Що додати | Очікуваний ефект | Складність |
|---|---|---|
| **Відгуки учнів** (3–5, з іменем і балом НМТ) | найсильніший соціальний доказ | потрібен контент |
| **Результати учнів** (скільки склали на 180+, куди вступили) | підтверджує обіцянку hero | потрібен контент |
| **Досвід і освіта викладача** (стаж, ЗВО, категорія) | довіра батьків, які платять | потрібен контент |
| **Ціна або «від N грн»** | відсіює нецільові дзвінки, економить час | рішення викладача |
| **Формат і графік** (онлайн/офлайн, тривалість, міні-групи) | знімає головне заперечення | 30 хв роботи |
| Години, коли зручно телефонувати | менше пропущених дзвінків | 5 хв роботи |

---

## 8. Технічні ризики та рекомендації

### Перед публікацією (обов'язково)

1. **Замінити домен** у `canonical`, `og:url`, `robots.txt`, `sitemap.xml`.
2. **Перевірити Telegram-посилання.** `t.me/+380680772607` працює лише якщо номер
   зареєстрований у Telegram і налаштування приватності це дозволяють.
   Надійніше — юзернейм: `https://t.me/username`.
3. **Увімкнути gzip/brotli на хостингу** — текстові файли стиснуться з 60 КБ до 16 КБ.
4. **HTTPS + HTTP/2**, кешування `assets/*` на рік (`Cache-Control: immutable`).

### Оптимізація (бажано)

5. **Фото → WebP + `srcset`.** Зараз 59 КБ JPEG 1024px, а показується максимум 440px.
   Версія 880px WebP важитиме ~22 КБ — мінус 60% від ваги фото.
6. **Самохостинг шрифтів.** Прибирає 2 зовнішні домени (DNS + TLS ≈ 200–400 мс на 3G),
   усуває ризик CLS і залежність від доступності Google Fonts.
7. **Лічильник аналітики** (GA4 або Plausible) з відстеженням кліків по `tel:` як конверсій —
   інакше неможливо оцінити ефективність сайту. Без форм cookie-банер не потрібен
   лише для Plausible/безкукового режиму.

### Сумісність браузерів

| Технологія | Підтримка | Що буде у старому браузері |
|---|---|---|
| `color-mix()` | Chrome 111+, Safari 16.2+ (2023) | іконки-плашки без заливки, верстка ціла |
| `backdrop-filter` | усі актуальні | шапка без розмиття, фон лишається |
| `mix-blend-mode` | усі актуальні | фото без м'ятної віньєтки |
| `viber://` | лише з установленим Viber | на десктопі без Viber посилання не спрацює |
| `env(safe-area-inset)` | iOS 11+ | звичайний відступ |

Критичних залежностей немає: у будь-якому браузері з 2020 року сайт читабельний і кнопки працюють.

### Приватність і безпека

- Сайт **не збирає жодних персональних даних** (немає форм, cookie, трекерів) —
  політика конфіденційності юридично не потрібна.
- Зовнішні посилання мають `rel="noopener"`.
- Немає інлайн-обробників подій — сумісно з суворим CSP, якщо хостинг його вмикає.

---

## 9. Що змінилося під час перевірки

| # | Проблема | Виявлено | Виправлено |
|---|---|---|---|
| 1 | Ім'я «Ольга Журавель» рендерилось суцільними плашками замість градієнтного тексту | скріншот десктопа | шорткат `background` скидав `background-clip:text` → `background-image` |
| 2 | Посилання в блоці «Напрямки» виходило за екран на 69px і обрізалось | вимір на 390px | прибрано `nowrap`, номер обгорнуто в `.nb` |
| 3 | Підписи карток мали контраст 2,5:1 (провал WCAG AA) | розрахунок контрасту | введено `--accent-ink` — темні кольори для тексту |
| 4 | «ПРО ВИКЛАДАЧА» / посилання-дзвінок: контраст 3,5:1 | розрахунок контрасту | `#0E9B87` → `#0B7D6D` (5,0:1) |
| 5 | Посилання футера мали тап-зону 37px | вимір на 390px | збільшено відступи до 44px |
| 6 | Клітина в hero перекривала спіраль ДНК | скріншот | зміщено координати SVG-групи |
| 7 | CSS-анімації скидали SVG-атрибути `transform` (графіка стрибала б у кут) | код-рев'ю | групи розділено: позиція зовні, анімація всередині |
| 8 | Чергування темних і світлих смуг виглядало рвано | зауваження замовника | єдина світла тема; темним лишився один акцент — фінальна картка; шапка, підвал і hero переведені у світле |
| 9 | Клік по номеру на комп'ютері викликав системне вікно Windows «чим відкрити посилання» | зауваження замовника | на пристроях із мишею клік копіює номер і показує підказку; на телефонах `tel:` працює як раніше |

---

## 10. Пріоритетний план дій

**Зараз (до публікації):** реальний домен → перевірка Telegram-посилання → gzip на хостингу.

**Перший тиждень:** лічильник аналітики з ціллю «клік по телефону» → блок «Формат і графік»
→ згадка міста/«онлайн» у текстах для гео-запитів.

**Перший місяць:** 3–5 відгуків учнів → результати НМТ минулих років → FAQ (8–10 питань,
+ розмітка `FAQPage`) → WebP-версія фото та самохостинг шрифтів.
